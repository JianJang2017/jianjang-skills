# 复制此文件为 .env 并填入真实信息。.env 已被仓库根 .gitignore 忽略，不会提交。
# 本 skill 的飞书信息都从 .env 读取，避免每次对话重复指定。
# 所有字段都是可选的：留空时回退到交互式指定（目标库）或 lark-cli 既有配置（凭证）。

# ───────────────────────────────────────────────
# 应用凭证（可选）
# ───────────────────────────────────────────────
# 缺省时沿用 lark-cli 已初始化的应用（~/.lark-cli/config.json，通过 lark-cli config init 配置）。
# 仅当你想用 .env 里另一套应用凭证时才填。App Secret 是敏感信息，切勿提交到仓库。
FEISHU_APP_ID=
FEISHU_APP_SECRET=
# 品牌：feishu（默认）| lark（国际版）
FEISHU_BRAND=feishu

# ───────────────────────────────────────────────
# 默认身份
# ───────────────────────────────────────────────
# 知识库/文档是个人资源，默认 user；仅当确实要用机器人身份时改为 bot。
FEISHU_AS=user

# ───────────────────────────────────────────────
# 默认目标知识库（三选一，优先级 SPACE_ID > WIKI_URL > SPACE_NAME）
# ───────────────────────────────────────────────
# 三者都留空 = 未指定知识库，skill 会按文档 H1/标题自动新建知识空间。
# 知识空间 ID（最精确，形如 7xxxxxxxxxxxxxxxxxx）
FEISHU_SPACE_ID=
# 知识库任一节点的 /wiki/ 链接，skill 会从中解析出 space_id
FEISHU_WIKI_URL=
# 知识空间名称（精确匹配；匹配到多个时会让你选）
FEISHU_SPACE_NAME=

# 可选：迁入文档时挂到的父节点 token（留空则挂在知识库根）
FEISHU_PARENT_NODE_TOKEN=
